<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vkyoungcn.smartdevices.yomemory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_6"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_6_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogoPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".GroupsOfMissionActivity" />
        <activity android:name=".ItemsOfMissionActivity" />
        <activity android:name=".GroupDetailActivity" />
        <activity android:name=".LearningActivity" />
        <activity android:name=".AccomplishActivity" />
        <activity android:name=".PrepareForLearningActivity" />
        <activity android:name=".MissionDetailsActivity" />
        <activity android:name=".ConfigurationActivity" />
        <activity android:name=".ItemDetailActivity"></activity>
    </application>

</manifest>